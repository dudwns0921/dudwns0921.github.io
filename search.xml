<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Debounce and Throttle</title>
    <url>/2022/10/06/Debounce-and-Throttle/</url>
    <content><![CDATA[<p>검색어 자동완성 기능을 만들면서 <strong>Debounce</strong>에 대해 알게 되었고, 자연스럽게 <strong>Throttle</strong>을 접하게 되었다. 먼저 두 기술이 왜 등장하게 되었는지부터 알아보자.</p>
<h1 id="등장배경"><a href="#등장배경" class="headerlink" title="등장배경"></a>등장배경</h1><p>자, 우리가 영화 정보를 검색할 수 있는 사이트에서  <strong>Matrix</strong> 시리즈를 보고 싶어 검색창에 <strong>Matrix</strong> 를 입력했다고 가정해보자. 해당 사이트에는 자동완성 기능을 제공하고 있는데, 자동완성 기능의 흐름은 다음과 같다.</p>
<ol>
<li>사용자가 글자를 입력함</li>
<li>입력할 때마다 서버에 새로운 요청을 보내 해당 검색어와 관련된 키워드들을 가져옴</li>
</ol>
<p>위 흐름대로 자동완성 기능이 수행될 때 결과를 콘솔에 출력해봤다.</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">search keyword : m / api request 1</span><br><span class="line">search keyword : ma / api request 2</span><br><span class="line">search keyword : mat / api request 3</span><br><span class="line">search keyword : matr / api request 4</span><br><span class="line">search keyword : matri / api request 5</span><br><span class="line">search keyword : matrix / api request 6</span><br></pre></td></tr></table></figure>

<p>우리는 <strong>matrix</strong>라는 6글자를 입력했을 뿐인데, 6개의 <strong>request</strong>가 서버로 보내졌다 . 만약 더 긴 제목의 영화를 검색한다면 영화 제목을 입력하는 짧은 시간 동안 수십 개의 <strong>request</strong> 가 발생하는 것이다. 이처럼 너무 많은 이벤트 횟수의 실행으로 이벤트 핸들러가 과도한 연산을 수행하는 경우 성능 문제가 발생하고 이는 사용자 경험까지 떨어뜨리게 된다.</p>
<p>여기서 필요한 것이 바로 <strong>Debounce</strong>와 <strong>Throttle</strong>이다. 두 기술 모두 자바스크립트의 이벤트를 제어(제한)해 성능을 향상시키는 방법이다.</p>
<h1 id="Debounce"><a href="#Debounce" class="headerlink" title="Debounce"></a>Debounce</h1><p><strong>Debounce</strong>는 이벤트를 그룹화해서 특정시간이 지난 후 하나의 이벤트만 발생하도록 하는 기술이다. 예시로 살펴보자.</p>
<ol>
<li>특정시간(delay)을 1초로 설정</li>
<li><strong>matrix</strong> 를 입력(각 글자를 입력하는데 설정한 시간 미만으로 걸린다고 가정)</li>
<li>설정한 시간 지남</li>
</ol>
<p>위 과정대로 진행되면 <strong>matrix</strong>라는 검색어와 함께 요청이 한 번만 발생한다. 원리는 다음과 같다.</p>
<ol>
<li>이벤트 발생 후 설정한 시간이 지나기 전에 이벤트가 다시 발생하면 지나간 시간을 초기화한다.</li>
<li>설정한 시간이 지나게 되면 연산을 수행한다.</li>
</ol>
<p>그러니까 만약 <strong>ma</strong>를 입력하고 1초가 지났다면 다음과 같은 결과가 나올 것이다.</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">search keyword : ma / api request 1</span><br></pre></td></tr></table></figure>

<p>어느 정도 개념에 대한 이해가 이루어졌다면 이제 <strong>Debounce</strong> 기술을 직접 구현해보자.</p>
<h2 id="Debounce-구현하기"><a href="#Debounce-구현하기" class="headerlink" title="Debounce 구현하기"></a>Debounce 구현하기</h2><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">&quot;text&quot;</span> /&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">b</span>&gt;</span>Default:<span class="tag">&lt;/<span class="name">b</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">span</span> <span class="attr">id</span>=<span class="string">&quot;default&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">b</span>&gt;</span>Debounce:<span class="tag">&lt;/<span class="name">b</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">span</span> <span class="attr">id</span>=<span class="string">&quot;debounce&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p><strong>Debounce</strong>가 적용됐을 때랑 적용되지 않았을 때의 차이를 좀 더 명확하게 확인하기 위해 위와 같이 <strong>HTML</strong> 구조를 짰다. </p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> input = <span class="variable language_">document</span>.<span class="title function_">querySelector</span>(<span class="string">&#x27;input&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> defaultText = <span class="variable language_">document</span>.<span class="title function_">querySelector</span>(<span class="string">&#x27;#default&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> debounceText = <span class="variable language_">document</span>.<span class="title function_">querySelector</span>(<span class="string">&#x27;#debounce&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> debounceReqCnt = <span class="number">0</span></span><br><span class="line"><span class="keyword">let</span> defaultReqCnt = <span class="number">0</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> updateDebounceText = <span class="title function_">debounce</span>(<span class="function">(<span class="params">text</span>) =&gt;</span> &#123;</span><br><span class="line">  debounceText.<span class="property">textContent</span> = text</span><br><span class="line">  debounceReqCnt++</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(</span><br><span class="line">    <span class="string">`[Debounce] search keyword : <span class="subst">$&#123;text&#125;</span> / api request <span class="subst">$&#123;debounceReqCnt&#125;</span>`</span>,</span><br><span class="line">  )</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> <span class="title function_">updateDefaultText</span> = (<span class="params">text</span>) =&gt; &#123;</span><br><span class="line">  defaultText.<span class="property">textContent</span> = text</span><br><span class="line">  defaultReqCnt++</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">`[Default] search keyword : <span class="subst">$&#123;text&#125;</span> / api request <span class="subst">$&#123;defaultReqCnt&#125;</span>`</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">input.<span class="title function_">addEventListener</span>(<span class="string">&#x27;input&#x27;</span>, <span class="function">(<span class="params">e</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="title function_">updateDefaultText</span>(e.<span class="property">target</span>.<span class="property">value</span>)</span><br><span class="line">  <span class="title function_">updateDebounceText</span>(e.<span class="property">target</span>.<span class="property">value</span>)</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">debounce</span>(<span class="params">cb, delay = <span class="number">1000</span></span>) &#123;</span><br><span class="line">  <span class="keyword">let</span> timeout</span><br><span class="line">  <span class="keyword">return</span> <span class="function">(<span class="params">arg</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="built_in">clearTimeout</span>(timeout)</span><br><span class="line">    timeout = <span class="built_in">setTimeout</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">      <span class="title function_">cb</span>(arg)</span><br><span class="line">    &#125;, delay)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>전체 코드는 위와 같다.</p>
<ol>
<li><strong>input</strong>에 <strong>addEventListener</strong> 를 적용</li>
<li><strong>input</strong> 이벤트가 발생할 때마다 <strong>DefaultText</strong>와 <strong>debounceText</strong>가 업데이트됨</li>
</ol>
<p>실행 흐름 자체는 그렇게 복잡하지 않다. 자세히 봐야할 부분은 맨 밑에 정의된 <strong>debounce</strong>함수이다.</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">debounce</span>(<span class="params">cb, delay = <span class="number">1000</span></span>) &#123;</span><br><span class="line">  <span class="keyword">let</span> timeout</span><br><span class="line">  <span class="keyword">return</span> <span class="function">(<span class="params">arg</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="built_in">clearTimeout</span>(timeout)</span><br><span class="line">    timeout = <span class="built_in">setTimeout</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">      <span class="title function_">cb</span>(arg)</span><br><span class="line">    &#125;, delay)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>debounce</strong>함수가 실행되면 기존에 <strong>setTimeout</strong>으로 등록되었던 타이머를 없애고 새로운 타이머를 등록한다. 이는 아래 내용을 구현한 것이라고 이해하면 된다.</p>
<blockquote>
<p><strong>이벤트 발생 후 설정한 시간이 지나기 전에 이벤트가 다시 발생하면 지나간 시간을 초기화한다.</strong></p>
</blockquote>
<p>그러면 이제 실제 실행 화면을 확인해보자.</p>
<img width="100%" src="/2022/10/06/Debounce-and-Throttle/debounceExample.gif">

<p><strong>Debounce</strong>를 쓰지 않은 경우 아까와 동일하게 6번의 <strong>request</strong>가 발생했고, <strong>Debounce</strong>를 적용시킨 경우 단 한 번의 <strong>request</strong>만이 발생했다. 수치상으로 6배의 성능 향상을 확인할 수 있다. 그럼 이제 <strong>Throttle</strong>에 대해서 알아보자.</p>
<h1 id="Throttle"><a href="#Throttle" class="headerlink" title="Throttle"></a>Throttle</h1><p><strong>Throttle</strong>은 이벤트를 일정한 주기마다 발생하도록 하는 기술이다. 예를 들어 <strong>Throttle</strong>의 설정시간으로 <strong>1ms</strong>를 주면 해당 이벤트는 <strong>1ms</strong> 동안 한 번만 발생하게 된다. <strong>Throttle</strong>을 사용하는 대표적인 예시로는 무한 스크롤 페이지가 있다. </p>
<blockquote>
<p><a href="https://www.agoda.com/ko-kr/?cid=1844104">https://www.agoda.com/ko-kr/?cid=1844104</a></p>
</blockquote>
<p>최근 파리에서 묵을 숙소를 찾으면서 사용한 <strong>agoda</strong>이다. 원하는 도시를 검색하고 계속 스크롤을 내리면 <strong>footer</strong>에 도달하기 전에 추가로 숙소 정보들을 가져오고 있는 걸 확인할 수 있다. 정확하지는 않을 수도 있지만 아마도 다음과 같은 흐름으로 실행되고 있을 거라고 생각한다.</p>
<ol>
<li>스크롤 위치가 <strong>footer</strong>와 얼마나 떨어져 있는지 확인</li>
<li>일정 거리 이하라면 콘텐츠를 가져와 페이지를 업데이트함</li>
</ol>
<p>스크롤을 조작하면 스크롤 이벤트가 굉장히 많이 발생한다. 그래서 만약 스크롤 이벤트가 발생할 때마다 <strong>footer</strong>와의 거리를 계산한다면 성능상의 이슈가 생길 수도 있다. 그럴 때 *Throttle을 사용할 수 있다. 연산이 몇 초에 한 번, 또는 몇 밀리초에 한 번씩만 수행되도록 제한을 두는 것이다.</p>
<h2 id="Throttle-구현하기"><a href="#Throttle-구현하기" class="headerlink" title="Throttle 구현하기"></a>Throttle 구현하기</h2><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">nav</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">div</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">b</span>&gt;</span>Default:<span class="tag">&lt;/<span class="name">b</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">span</span> <span class="attr">id</span>=<span class="string">&quot;default&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">div</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">b</span>&gt;</span>Throttle:<span class="tag">&lt;/<span class="name">b</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">span</span> <span class="attr">id</span>=<span class="string">&quot;throttle&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">nav</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">body</span> &#123;</span><br><span class="line">    <span class="attribute">height</span>: <span class="number">300vh</span>;</span><br><span class="line">    <span class="attribute">margin</span>: <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="selector-tag">nav</span> &#123;</span><br><span class="line">    <span class="attribute">position</span>: fixed;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>Debounce</strong>와 마찬가지로 <strong>Throttle</strong>을 적용했을 때와 적용하지 않았을 때의 차이를 보기 위해 위와 같이 html 구조를 짰다.</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> defaultText = <span class="variable language_">document</span>.<span class="title function_">querySelector</span>(<span class="string">&#x27;#default&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> throttleText = <span class="variable language_">document</span>.<span class="title function_">querySelector</span>(<span class="string">&#x27;#throttle&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> defaultCnt = <span class="number">0</span></span><br><span class="line"><span class="keyword">let</span> throttleCnt = <span class="number">0</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> <span class="title function_">countDetectingDefault</span> = (<span class="params"></span>) =&gt; &#123;</span><br><span class="line">  defaultCnt++</span><br><span class="line">  defaultText.<span class="property">textContent</span> = defaultCnt</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> countDetectingThrottle = <span class="title function_">throttle</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">  throttleCnt++</span><br><span class="line">  throttleText.<span class="property">textContent</span> = throttleCnt</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line"><span class="variable language_">window</span>.<span class="title function_">addEventListener</span>(<span class="string">&#x27;scroll&#x27;</span>, <span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">  <span class="title function_">countDetectingDefault</span>()</span><br><span class="line">  <span class="title function_">countDetectingThrottle</span>()</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">throttle</span>(<span class="params">cb, delay = <span class="number">1000</span></span>) &#123;</span><br><span class="line">  <span class="keyword">let</span> shouldWait = <span class="literal">false</span></span><br><span class="line">  <span class="keyword">return</span> <span class="function">(<span class="params">...args</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (shouldWait) <span class="keyword">return</span></span><br><span class="line">    <span class="title function_">cb</span>(...args)</span><br><span class="line">    shouldWait = <span class="literal">true</span></span><br><span class="line">    <span class="built_in">setTimeout</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">      shouldWait = <span class="literal">false</span></span><br><span class="line">    &#125;, delay)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ol>
<li><strong>winodw</strong>객체에 <strong>addEventListener</strong> 를 적용</li>
<li>스크롤 이벤트가 발생할 때마다 카운트 증가</li>
</ol>
<p>따로 구현은 하지 않았지만 여기서 스크롤 이벤트가 발생한다는 건 아래의 작업이 이루어지는 거라고 가정하자.</p>
<blockquote>
<p><strong>스크롤 위치가 footer와 얼마나 떨어져 있는지 확인</strong></p>
</blockquote>
<p><strong>Debounce</strong>와 마찬가지로 가장 중요한 건 맨 밑의 <strong>throttle</strong> 함수이다.</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">throttle</span>(<span class="params">cb, delay = <span class="number">1000</span></span>) &#123;</span><br><span class="line">  <span class="keyword">let</span> shouldWait = <span class="literal">false</span></span><br><span class="line">  <span class="keyword">return</span> <span class="function">(<span class="params">arg</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (shouldWait) <span class="keyword">return</span></span><br><span class="line">    <span class="title function_">cb</span>(arg)</span><br><span class="line">    shouldWait = <span class="literal">true</span></span><br><span class="line">    <span class="built_in">setTimeout</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">      shouldWait = <span class="literal">false</span></span><br><span class="line">    &#125;, delay)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ol>
<li><strong>shouldWait</strong> 의 초기값이 <strong>false</strong>이기 때문에 최초에는 콜백함수가 무조건 실행된다.</li>
<li>콜백 함수가 실행됐다면 <strong>shouldWait</strong>의 값을 <strong>true</strong>로 바꾼다.</li>
<li><strong>setTimeout</strong>을 통해 설정한 시간 뒤에 <strong>shouldWait</strong>의 값이 바뀌도록 한다.</li>
<li>설정한 시간이 지나기 전까지는 <strong>shouldWait</strong>의 값이 <strong>true</strong>이기 때문에 연산이 수행되지 않는다.</li>
</ol>
<p>실행 화면을 확인해보자.</p>
<img width="100%" src="/2022/10/06/Debounce-and-Throttle/ThrottleExample.gif">

<p>페이지의 처음부터 끝까지 스크롤하는 동안 <strong>Throttle</strong>을 적용하지 않았을 때는 스크롤이 <strong>footer</strong>로부터 얼마나 떨어져 있는지 계산하는 연산이 총 67번 수행됐고, <strong>Throttle</strong>을 적용했을 경우에는 단 2번만 수행됐다. </p>
<h1 id="💡-Tip-Debounce과-Throttle의-차이"><a href="#💡-Tip-Debounce과-Throttle의-차이" class="headerlink" title="💡 Tip - Debounce과 Throttle의 차이"></a>💡 Tip - <strong>Debounce</strong>과 <strong>Throttle</strong>의 차이</h1><p><strong>Debounce</strong>과 <strong>Throttle</strong>의 가장 큰 차이점은 <strong>Throttle</strong>은 설정한 시간마다 연산이 수행됨을 보장한다는 것이다. 그래서 <strong>Throttle</strong>은 지속적인 업데이트가 필요한 경우에 사용하는 게 좋다.</p>
<p>만약 무한 스크롤 페이지를 구현할 때 <strong>Debounce</strong>를 사용했다고 가정해보자. 스크롤 이벤트가 멈춰야만 <strong>footer</strong>와의 거리를 확인하고 콘텐츠를 추가로 가져올 것이기 때문에 <strong>footer</strong>에 도달하더라도 스크롤 이벤트가 설정한 시간 내에 다시 발생한다면 아무런 변화도 일어나지 않을 것이다.</p>
<p>하지만 <strong>Throttle</strong>은 정기적으로 연산이 실행되기 때문에 위의 문제가 발생하지 않는다. 설정한 시간마다 연산을 수행하기 때문에 <strong>footer</strong>와의 거리를 지속적으로 업데이트할 수 있어 <strong>footer</strong>에 도달하기 전에 정상적으로 콘텐츠를 추가로 가져올 수 있을 것이다.</p>
<h1 id="📚-참고자료"><a href="#📚-참고자료" class="headerlink" title="📚 참고자료"></a>📚 참고자료</h1><p><a href="https://webclub.tistory.com/607">https://webclub.tistory.com/607</a></p>
<p><a href="https://www.youtube.com/watch?v=cjIswDCKgu0">https://www.youtube.com/watch?v=cjIswDCKgu0</a></p>
]]></content>
      <categories>
        <category>Javascript</category>
      </categories>
      <tags>
        <tag>Debounce</tag>
        <tag>Throttle</tag>
      </tags>
  </entry>
</search>
